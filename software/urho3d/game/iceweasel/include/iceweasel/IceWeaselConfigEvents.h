#pragma once

#include <Urho3D/Core/Object.h>

/// Mouse click on a UI element. Parameters are same as in UIMouseClick event, but is sent by the element.
URHO3D_EVENT(E_CONFIGRELOADED, ConfigReloaded)
{
}
